{
  "name": "@repo/lambda",
  "version": "0.0.1",
  "main": "index.ts",
  "types": "index.ts",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "yarn prebuild && find . -name '*.lambda.ts' -exec sh -c 'ENVIRONMENT=$(basename {} .lambda.ts) && FUNCTION=$(basename $(dirname {})) && yarn esbuild {} --bundle --minify --sourcemap --platform=node --outfile=dist/$ENVIRONMENT/$FUNCTION/index.js' \\;",
    "postbuild": "find dist/*/* -type d -exec sh -c 'cd {} && zip -r index.zip *' \\;",
    "build:lambda": "yarn prebuild && find . -name 'lambda.ts' -exec sh -c 'FUNCTION=$(basename $(dirname {})) && ENV=${LAMBDA_ENV:-testnet-staging} && yarn esbuild {} --bundle --minify --sourcemap --platform=node --define:process.env.LAMBDA_ENV=\"\\\"$ENV\\\"\" --outfile=dist/$ENV/$FUNCTION/index.js' \\;",
    "postbuild:lambda": "find dist/*/*/ -name 'index.js' -exec sh -c 'cd $(dirname {}) && zip -r index.zip *' \\;",
    "test": "yarn build && TEST_ENVIRONMENT=testnet jest",
    "start": "npx ts-node",
    "simulate:local:signal-bot-user": "TEST_ENVIRONMENT=local npx ts-node src/helpers/simulate/ve-better-passport/signalBotUser.ts"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1050.0",
    "@vechain/vebetterdao-contracts": "*",
    "lambda-local": "^2.1.2"
  },
  "devDependencies": {
    "@slack/web-api": "^7.0.4",
    "@types/aws-lambda": "^8.10.161",
    "@vechain/sdk-core": "2.0.7",
    "@vechain/sdk-network": "2.0.7",
    "aws-lambda": "^1.0.7",
    "eslint-plugin-prettier": "^5.5.5",
    "ethers": "^6.11.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.9"
  }
}
