name: Unit Testing

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - testnet
      - feat/reduce-contracts-size
      - feat/navigators
      - feat/phase-2
  push:
    branches:
      - main
      - testnet

jobs:
  check-changes:
    runs-on: ubuntu-latest
    outputs:
      frontend-changed: ${{ steps.changes.outputs.frontend }}
      contracts-changed: ${{ steps.changes.outputs.contracts }}
      utils-changed: ${{ steps.changes.outputs.utils }}
      backend-changed: ${{ steps.changes.outputs.backend }}
      lambda-changed: ${{ steps.changes.outputs.lambda }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Check for Changes
        id: changes
        uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d # v4.0.1
        with:
          filters: |
            frontend:
              - 'apps/frontend/**'
            contracts:
              - 'packages/contracts/**'
            utils:
              - 'packages/utils/**'
            backend:
              - 'apps/backend/**'
            lambda:
              - 'packages/lambda/**'

  run-unit-tests:
    name: Unit Testing
    runs-on: ubuntu-latest
    needs: check-changes
    # Job always runs so the required "Unit Testing" check succeeds; steps are skipped when no path matched.
    env:
      UNIT_SHOULD_RUN: ${{ needs.check-changes.outputs.frontend-changed == 'true' || needs.check-changes.outputs.contracts-changed == 'true' || needs.check-changes.outputs.utils-changed == 'true' || needs.check-changes.outputs.backend-changed == 'true' || needs.check-changes.outputs.lambda-changed == 'true' }}
    steps:
      - name: Skip (no matching paths)
        if: env.UNIT_SHOULD_RUN != 'true'
        run: echo "::notice::No apps/contracts/utils/backend/lambda paths changed — skipping install and tests."

      - name: Checkout repository
        if: env.UNIT_SHOULD_RUN == 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Use Node v20
        if: env.UNIT_SHOULD_RUN == 'true'
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: .nvmrc
          cache: "yarn"

      - name: Cache node_modules
        if: env.UNIT_SHOULD_RUN == 'true'
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            node_modules
            packages/*/node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/yarn.lock') }}

      # Restore when enabling steps that involve smart contract compilation
      # - name: Cache Solidity compilers
      #   uses: actions/cache@v3
      #   with:
      #     path: ~/.cache/hardhat-nodejs
      #     key: ${{ runner.os }}-solc-${{ hashFiles('./packages/contracts/hardhat.config.ts') }}
      #     restore-keys: |
      #       ${{ runner.os }}-solc-

      - name: Install dependencies
        if: env.UNIT_SHOULD_RUN == 'true'
        run: yarn install --frozen-lockfile

      # Disabled Global and Frontend unit tests for now as they are not working
      # Contract tests should be run separately for each shard on the next step

      # Run global unit tests if contract or utils changes are detected
      # - name: Global Unit Tests
      #   if: needs.check-changes.outputs.contracts-changed == 'true' || needs.check-changes.outputs.utils-changed == 'true'
      #   run: yarn test

      # Run Frontend unit tests if frontend, contracts, or utils changes are detected
      # - name: Frontend Unit Tests
      #   if: needs.check-changes.outputs.frontend-changed == 'true' || needs.check-changes.outputs.contracts-changed == 'true' || needs.check-changes.outputs.utils-changed == 'true'
      #   run: yarn test --filter=frontend

      # Run Backend unit tests only if there are backend changes and no contracts/utils changes
      - name: Backend Unit Tests
        if: env.UNIT_SHOULD_RUN == 'true' && needs.check-changes.outputs.backend-changed == 'true' && needs.check-changes.outputs.contracts-changed != 'true' && needs.check-changes.outputs.utils-changed != 'true'
        run: yarn test --filter=backend

      # Run Lambda unit tests only if there are lambda changes and no contracts/utils changes
      # - name: Lambda Unit Tests
      #   if: needs.check-changes.outputs.lambda-changed == 'true' && needs.check-changes.outputs.contracts-changed != 'true' && needs.check-changes.outputs.utils-changed != 'true'
      #   run: yarn test --filter=lambda

  run-contract-tests:
    # Use shard in the job name (ASCII-only); labels contain & and long text and break check title interpolation.
    name: SC Test (${{ matrix.shard }})
    runs-on: ubuntu-latest
    needs: check-changes
    if: needs.check-changes.outputs.contracts-changed == 'true'
    env:
      NODE_OPTIONS: --max-old-space-size=8192
    strategy:
      fail-fast: false
      matrix:
        include:
          - shard: shard0
            label: B3TR Token & Multi Sig
          - shard: shard2
            label: Emissions
          - shard: shard3a
            label: Galaxy Member
          - shard: shard3b
            label: Galaxy Member - V6 Upgrade
          - shard: shard3c
            label: Galaxy Member - V6 Compatibility
          - shard: shard4a1
            label: Governor & TimeLock - Part 1
          - shard: shard4a2
            label: Governor & TimeLock - Part 2
          - shard: shard4b
            label: Governance - Voting power with proposal deposit
          - shard: shard4c
            label: Governance - Milestone Creation
          - shard: shard4d
            label: Governance - Proposer Requirement
          - shard: shard4e
            label: Governance - V7 Compatibility & Thresholds
          - shard: shard4f
            label: Governance - V7 Upgrade
          - shard: shard4g
            label: Governance - V8 Upgrade
          - shard: shard4h
            label: Governance - V8 Compatibility
          - shard: shard4i
            label: Governance - Grants Manager V2 Upgrade
          - shard: shard4j
            label: Governance - Grants Manager V2 Compatibility
          - shard: shard4k
            label: Governance - Grants Manager V3 Metadata URI Access
          - shard: shard4z
            label: Governance - Community Execution Framework V11
          - shard: shard6
            label: TimeLock
          - shard: shard7
            label: Treasury
          - shard: shard7b
            label: Dynamic Base Allocation Pool
          - shard: shard8-core
            label: VeBetterPassport - Core
          - shard: shard8a
            label: VeBetterPassport - Upgrade
          - shard: shard8b
            label: VeBetterPassport - Signaling
          - shard: shard8c
            label: VeBetterPassport - Reset Signal Count
          - shard: shard8d
            label: VeBetterPassport - Upgrades
          - shard: shard8e
            label: VeBetterPassport - Entities
          - shard: shard8f
            label: VeBetterPassport - Delegation
          - shard: shard8g
            label: VeBetterPassport - V6 Actor-Keyed Counts
          - shard: shard9
            label: VOT3
          - shard: shard9a
            label: B3TRChallenges
          - shard: shard9b
            label: B3TRChallenges - Passport gating
          - shard: shard9c
            label: B3TRChallenges - V2 Upgrade
          - shard: shard9d
            label: B3TRChallenges - V2 Compatibility
          - shard: shard10-core
            label: VoterRewards
          - shard: shard10a
            label: VoterRewards - Upgrade
          - shard: shard10b
            label: VoterRewards - Relayer Claim Rewards
          - shard: shard11
            label: X2EarnCreator
          - shard: shard12
            label: X2EarnRewardsPool
          - shard: shard13
            label: X-Allocation Pool
          - shard: shard14-core
            label: X-Allocation Voting
          - shard: shard14a
            label: X-Allocation Voting - Upgrade
          - shard: shard14b
            label: X-Allocation Voting - Auto Voting
          - shard: shard14c
            label: X-Allocation Voting - Double Process Prevention
          - shard: shard15a
            label: X-Apps - Core Features
          - shard: shard15b
            label: X-Apps - Team Management
          - shard: shard15c
            label: X-Apps - Metadata and Endorsement
          - shard: shard15d
            label: X-Apps - V7 Upgrade
          - shard: shard15e
            label: X-Apps - Upgradeability
          - shard: shard15f
            label: X-Apps - V8 Upgrade
          - shard: shard15g
            label: X-Apps - EndorsementUtils Coverage
          - shard: shard16-pop
            label: VeBetterPassport - PoP Score
          - shard: shard16a
            label: VeBetterPassport - Whitelisting
          - shard: shard16b
            label: VeBetterPassport - GM & Governance
          - shard: shard17a
            label: X-Apps Additional Tests A
          - shard: shard17b
            label: X-Apps Additional Tests B
          - shard: shard18
            label: RelayerRewardsPool
          - shard: shard19a
            label: Navigator Registry - Core
          - shard: shard19b
            label: Navigator - Delegation
          - shard: shard19c
            label: Navigator - Voting
          - shard: shard19d
            label: Navigator - Fees
          - shard: shard19e
            label: Navigator - Slashing
          - shard: shard19f
            label: Navigator - Lifecycle
          - shard: shard19g
            label: Navigator - Integration
          - shard: shard19h
            label: Navigator - Security
          - shard: shard19i
            label: Navigator - Staked Voting Power
          - shard: shard20e2e
            label: Navigator - Relayer E2E
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Use Node v20
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: .nvmrc
          cache: "yarn"

      - name: Cache node_modules
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            node_modules
            packages/*/node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/yarn.lock') }}

      - name: Cache Solidity compilers
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.cache/hardhat-nodejs
          key: ${{ runner.os }}-solc-${{ hashFiles('./packages/contracts/hardhat.config.ts') }}
          restore-keys: |
            ${{ runner.os }}-solc-

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Set Shard Environment Variable
        run: echo "SHARD=${{ matrix.shard }}" >> $GITHUB_ENV

      - name: Run Unit Tests and Check Coverage for ${{ matrix.label }}
        run: yarn test:coverage:solidity

      - name: Upload Coverage for ${{ matrix.label }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-${{ matrix.shard }}
          path: packages/contracts/coverage/coverage-final.json

  combine-coverage:
    name: Combine Coverage Reports
    runs-on: ubuntu-latest
    needs: run-contract-tests
    steps:
      - name: Checkout Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Use Node v20
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: .nvmrc
          cache: "yarn"

      - name: Cache node_modules
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            node_modules
            packages/*/node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/yarn.lock') }}

      - name: Install Dependencies at Root
        run: yarn install --frozen-lockfile

      - name: Download coverage artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: coverage-*
          path: packages/contracts/coverage-shards

      - name: Debug Downloaded Artifacts
        run: ls -la packages/contracts/coverage-shards

      - name: Merge Coverage Reports
        working-directory: packages/contracts
        run: npx ts-node scripts/helpers/merge-coverage.ts

      - name: Create .nyc_output Directory
        run: mkdir -p packages/contracts/.nyc_output

      - name: Generate Final Coverage Report
        working-directory: packages/contracts
        run: yarn coverage:report

      - name: Upload Unified Coverage Report to Codecov
        uses: codecov/codecov-action@57e3a136b779b570ffcdbf80b3bdc90e7fab3de2 # v6.0.0
        with:
          directory: packages/contracts/coverage/
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
